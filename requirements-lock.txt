# Generated lock file — install with: pip install -r requirements-lock.txt
# For reproducible builds, pin exact versions after first successful install.
# Run: pip freeze > requirements-lock.txt  (inside .venv)
